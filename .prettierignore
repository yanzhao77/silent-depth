dist
coverage
node_modules
assets
reports
factory
package-lock.json
*.md

# UE4 工程生成物：可重建、不进版本库，且包含 UTF-16 等非 Prettier 可解析格式。
# 忽略范围与 ue4/SilentDepthUE/.gitignore 保持一致。
ue4/SilentDepthUE/.vs
ue4/SilentDepthUE/Binaries
ue4/SilentDepthUE/Build
ue4/SilentDepthUE/DerivedDataCache
ue4/SilentDepthUE/Intermediate
ue4/SilentDepthUE/Saved

# 资产契约与审计数据由 Blender/Python 管线生成，格式由生成器决定，
# 不纳入前端风格检查；同目录下的 .mjs 脚本仍然参与检查。
SilentDepth_Assets/**/*.json

# UE4 工程的资产清单同为工具生成产物
ue4/SilentDepthUE/Config/SilentDepth/submarine_manifest.json

# 科技树运行时数据由 tools/ue4/sync-tech-tree-data.mjs 从资产库原样复制，
# 格式由生成器决定，不做前端风格检查。
ue4/SilentDepthUE/Config/SilentDepth/TechTree
